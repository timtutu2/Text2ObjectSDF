# Text2ObjectSDF - full dependencies for Docker
# Base image provides: torch (CUDA), Python 3.10.
# Keep torch pinned so non-base environments still install a compatible version.

# From requirements.txt
numpy
trimesh
rtree
pysdf

# Training & inference (scripts, src)
PyYAML
tensorboard
wandb
tqdm
PyMCubes
# tinycudann: installed from GitHub in Dockerfile (not on PyPI)
transformers>=4.30,<5.0
scipy
scikit-image
torch==2.2.0
